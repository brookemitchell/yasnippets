# -*- mode: snippet -*-
# contributor: Brooke Mitchell
# name: reducer
# key: rred
# --
import { fromJS, List }

const initialState = fromJS({
      $2
})

function $1 (state = initialState, action) {
  switch (action.type) {
  case $2:
    return {
      ...state,
      $0
    }
  default:
    return state
  }
}
